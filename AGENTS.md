# 공통 작업 규칙

## 엔진과 클라이언트(게임) 구분

- 엔진과 클라이언트(게임)는 별도 영역으로 관리한다.
- 사용자가 "클라이언트"라고 하면 게임 쪽(`Survival`, 주로 `Survival\Asset`)을 수정하고, "엔진"이라고 하면 엔진·에디터 소스 쪽(`PixelSolution`)을 수정한다. 이 용어 구분을 기본 작업 범위로 적용한다.
- 엔진·에디터 소스: `D:\PixelEngine\PixelSolution`.
- 현재 게임 프로젝트: `D:\PixelEngine\Survival`. 게임 콘텐츠와 Lua 스크립트는 `Survival\Asset`에서 관리한다.
- 게임 프로젝트의 `Engine`은 엔진 제공 파일, `Editor`는 에디터 전용 데이터를 저장한다.
- `PixelSolution\Client`는 엔진 실행 진입점이며, 사용자가 말하는 클라이언트(게임 콘텐츠)와 구분한다.
- 게임 수정 요청은 해당 게임 폴더부터 확인한다. 엔진 수정이 필요한 경우에만 관련 엔진 소스로 범위를 넓힌다.
- 과거 게임 경로를 그대로 사용하지 말고 현재 프로젝트 경로를 기준으로 작업한다. 다른 게임을 추가해도 엔진과 게임별 콘텐츠를 분리한다.

## 토큰과 작업량 절약

- 답변은 한국어로 핵심 결과부터 짧게 작성한다. 같은 설명과 진행 상황을 반복하지 않는다.
- 먼저 요청과 관련된 폴더·파일·함수를 좁힌다. 저장소 전체 탐색이나 전체 파일 읽기는 필요한 경우에만 한다.
- 검색은 파일명과 키워드부터 시작하고, 필요한 코드 구간만 읽는다. 긴 로그·생성 파일·바이너리·빌드 결과물은 통째로 읽지 않는다.
- 이미 확인한 내용은 재사용하고, 변경 여부나 불확실성이 있을 때만 다시 확인한다.
- 엔진 구조·Lua 생성·PixelTool을 다룰 때만 `CODEX.md`의 관련 기술 기록을 참고한다. 관련 없는 스킬과 참고 문서는 읽지 않는다.
- 요청 범위 밖의 리팩터링, 추가 기능, 문서·임시 파일 생성은 하지 않는다. 기존 사용자 변경을 보존한다.
- 검증은 변경의 영향에 맞춰 필요한 범위로 수행한다. 문서만 수정한 경우 빌드하지 않고, 코드 변경에서는 필요한 검증을 생략하지 않는다.
- 결과는 변경 내용과 검증 결과, 남은 문제가 있으면 그 문제만 간단히 보고한다. 전체 코드와 도구 출력을 반복해서 붙이지 않는다.
- 토큰 절약을 이유로 필요한 조사·수정·검증을 생략하거나 요청을 미완료로 끝내지 않는다.
