# 2026-06-26 Engine Feedback

## 오늘의 피드백

> [!note] 에디터 · 미검토
> [[Object 창이 씬 변경 콜백을 받아도 목록을 갱신하지 못함]]
>
> 씬을 바꿔도 Object 창 목록과 선택 흐름이 실제 엔진 상태를 따라가지 못한다.

> [!note] 에디터 · 미검토
> [[Asset 파일 단건 삭제는 여전히 휴지통을 거치지 않는다]]
>
> 다중 삭제와 달리 단건 삭제는 즉시 영구 삭제돼 Asset 창 동작이 일관되지 않다.

> [!note] 에디터 · 미검토
> [[머티리얼 텍스처 찾기 시작 위치가 현재 프로젝트 Asset을 따르지 않음]]
>
> 머티리얼 생성 중 텍스처를 고를 때 현재 프로젝트 대신 실행 폴더 기준으로 탐색이 시작된다.

> [!note] 엔진 · 미검토
> [[Generate 콘솔의 3번 JSON 생성이 경로 없이 실행되고 바로 종료된다]]
>
> 코드 생성 도구에서 JSON 생성 메뉴가 잘못 연결돼 결과 파일 위치와 성공 여부를 확인하기 어렵다.

> [!note] 그래픽 · 미검토
> [[머티리얼 창은 .tga를 고르게 하지만 엔진 Import 단계는 .tga를 읽지 않는다]]
>
> 에디터가 허용하는 텍스처 확장자와 엔진의 실제 import 범위가 맞지 않는다.

> [!note] 에디터 · 미검토
> [[Asset 창 새 파일 만들기는 Lua·모듈·Scene 템플릿을 재사용하지 않는다]]
>
> Asset 창에서 만든 새 `.lua`, `.pxm`, `.scene` 파일이 툴바 생성 경로와 달리 빈 파일로만 떨어진다.

> [!note] 에디터 · 미검토
> [[씬 뷰를 여는 순간 main.lua와 BaseCamera.pxm이 조용히 생성된다]]
>
> 프로젝트를 열기만 해도 SceneView 초기화가 기본 스크립트와 카메라 파일을 자동으로 만들어 버린다.

> [!note] 엔진 · 미검토
> [[엔진 Clear 이후 재시작은 전달받은 프로젝트 루트 대신 상대경로 main.lua에 묶여 있다]]
>
> 엔진이 Clear 뒤 다시 시작할 때 설정된 루트 경로를 쓰지 않고 `./Asset/main.lua`를 고정으로 다시 읽는다.

> [!note] 에디터 · 미검토
> [[Asset 가져오기는 파일만 복사하고 실행 중 엔진 Import는 갱신하지 않는다]]
>
> 외부 파일을 Asset으로 가져와도 에디터 목록만 새로고침되고 실행 중 엔진 리소스 갱신은 보이지 않는다.

> [!note] 엔진 · 미검토
> [[Movement 모듈은 API가 보이지만 실제 이동 로직은 거의 비활성 상태다]]
>
> MoveToPosition 같은 호출은 남아 있지만 핵심 Update와 콜백 연결이 주석 처리돼 실제 움직임으로 이어지지 않는다.

## 상태 요약

| 상태 | 개수 |
| --- | ---: |
| 미검토 | 10 |
| 채택 | 0 |
| 폐기 | 0 |
| 일감화 | 0 |
