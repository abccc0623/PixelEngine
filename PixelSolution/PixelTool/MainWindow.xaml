<Window x:Class="PixelTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PixelTool" 
        xmlns:dock="https://github.com/Dirkster99/AvalonDock"
        mc:Ignorable="d"
        AllowsTransparency="False"
        Background="Transparent"
        Opacity="1.0"
        Title="PixelTool" Width="1920" Height="1080">


    <Grid Background="{DynamicResource PixelBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="65" />
        </Grid.ColumnDefinitions>


        <dock:DockingManager x:Name="dockManager" Grid.Row="0" Background="#191B1C2E" OpacityMask="Black" Foreground="#00000000" BorderBrush="#00000000" Margin="0" >

            <dock:LayoutRoot>
                <dock:LayoutPanel Orientation="Vertical">

                    <dock:LayoutPanel Orientation="Horizontal" DockWidth="0.5*" DockHeight="0.75*">
                        <dock:LayoutDocumentPane>
                            <dock:LayoutDocument Title="Scene View">
                                <local:GraphicsWindow x:Name="GraphicsWindow" />
                            </dock:LayoutDocument>
                        </dock:LayoutDocumentPane>

                        <dock:LayoutDocumentPane>
                            <dock:LayoutDocument Title="LuaEditor">
                                <local:LuaEditorWindow></local:LuaEditorWindow>
                            </dock:LayoutDocument>
                        </dock:LayoutDocumentPane>
                    </dock:LayoutPanel>

                    <dock:LayoutPanel Orientation="Horizontal" DockWidth="0.5*" DockHeight="0.25*">
                        <dock:LayoutDocumentPane DockWidth="0.75*" DockHeight="1.0*">
                            <dock:LayoutDocument Title="Asset">
                                <local:AssetWindow/>
                            </dock:LayoutDocument>
                        </dock:LayoutDocumentPane>

                        <dock:LayoutDocumentPane DockWidth="0.25*" DockHeight="1.0*">
                            <dock:LayoutDocument Title="Log">
                                <local:ConsoleWindow/>
                            </dock:LayoutDocument>
                        </dock:LayoutDocumentPane>
                    </dock:LayoutPanel>

                </dock:LayoutPanel>
            </dock:LayoutRoot>
        </dock:DockingManager>

        <Grid Grid.Column="1">
            <local:ToolbarWindow />
        </Grid>
    </Grid>
    
</Window>
