<UserControl x:Class="PixelTool.TransformView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PixelTool"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="InspectorTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3F3F3F"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#007ACC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid MinWidth="300" MaxWidth="300" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Border Background="{DynamicResource PixelBorderBrush}" 
                BorderBrush="#3F3F3F" BorderThickness="1" CornerRadius="3" 
                Padding="8" Margin="0,5" VerticalAlignment="Stretch">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="📍" Margin="0,0,8,0" VerticalAlignment="Center" Foreground="{DynamicResource PixelTextBrush}"/>
                    <TextBlock Text="TRANSFORM" FontWeight="Bold" Foreground="White" FontSize="12" VerticalAlignment="Center"/>
                </StackPanel>

                <Grid Grid.Row="1" VerticalAlignment="Top">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="75"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Position" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="5,5"/>
                    <UniformGrid Grid.Row="0" Grid.Column="1" Columns="3" Margin="0,5">
                        <DockPanel Margin="2,0">
                            <TextBlock Text="X" Width="12" Foreground="#FF4B4B" FontWeight="Bold" VerticalAlignment="Center" FontSize="10"/>
                            <TextBox Text="{Binding PositionX, UpdateSourceTrigger=PropertyChanged, TargetNullValue='0'}" Style="{StaticResource InspectorTextBoxStyle}" PreviewTextInput="NumberOnly_PreviewTextInput"/>
                        </DockPanel>
                        <DockPanel Margin="2,0">
                            <TextBlock Text="Y" Width="12" Foreground="#4BFF4B" FontWeight="Bold" VerticalAlignment="Center" FontSize="10"/>
                            <TextBox Text="{Binding PositionY, UpdateSourceTrigger=PropertyChanged, TargetNullValue='0'}" Style="{StaticResource InspectorTextBoxStyle}" PreviewTextInput="NumberOnly_PreviewTextInput"/>
                        </DockPanel>
                        <DockPanel Margin="2,0">
                            <TextBlock Text="Z" Width="12" Foreground="#4B4BFF" FontWeight="Bold" VerticalAlignment="Center" FontSize="10"/>
                            <TextBox Text="{Binding PositionZ, UpdateSourceTrigger=PropertyChanged, TargetNullValue='0'}" Style="{StaticResource InspectorTextBoxStyle}" PreviewTextInput="NumberOnly_PreviewTextInput"/>
                        </DockPanel>
                    </UniformGrid>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Rotation" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="5,5"/>
                    <UniformGrid Grid.Row="1" Grid.Column="1" Columns="3" Margin="0,5">
                        <DockPanel Margin="2,0">
                            <TextBlock Text="X" Width="12" Foreground="#FF4B4B" FontWeight="Bold" VerticalAlignment="Center" FontSize="10"/>
                            <TextBox Text="{Binding RotationX, UpdateSourceTrigger=PropertyChanged, TargetNullValue='0'}" Style="{StaticResource InspectorTextBoxStyle}" PreviewTextInput="NumberOnly_PreviewTextInput"/>
                        </DockPanel>
                        <DockPanel Margin="2,0">
                            <TextBlock Text="Y" Width="12" Foreground="#4BFF4B" FontWeight="Bold" VerticalAlignment="Center" FontSize="10"/>
                            <TextBox Text="{Binding RotationY, UpdateSourceTrigger=PropertyChanged, TargetNullValue='0'}" Style="{StaticResource InspectorTextBoxStyle}" PreviewTextInput="NumberOnly_PreviewTextInput"/>
                        </DockPanel>
                        <DockPanel Margin="2,0">
                            <TextBlock Text="Z" Width="12" Foreground="#4B4BFF" FontWeight="Bold" VerticalAlignment="Center" FontSize="10"/>
                            <TextBox Text="{Binding RotationZ, UpdateSourceTrigger=PropertyChanged, TargetNullValue='0'}" Style="{StaticResource InspectorTextBoxStyle}" PreviewTextInput="NumberOnly_PreviewTextInput"/>
                        </DockPanel>
                    </UniformGrid>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Scale" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="5,5"/>
                    <UniformGrid Grid.Row="2" Grid.Column="1" Columns="3" Margin="0,5">
                        <DockPanel Margin="2,0">
                            <TextBlock Text="X" Width="12" Foreground="#FF4B4B" FontWeight="Bold" VerticalAlignment="Center" FontSize="10"/>
                            <TextBox Text="{Binding ScaleX, UpdateSourceTrigger=PropertyChanged, TargetNullValue='1'}" Style="{StaticResource InspectorTextBoxStyle}" PreviewTextInput="NumberOnly_PreviewTextInput"/>
                        </DockPanel>
                        <DockPanel Margin="2,0">
                            <TextBlock Text="Y" Width="12" Foreground="#4BFF4B" FontWeight="Bold" VerticalAlignment="Center" FontSize="10"/>
                            <TextBox Text="{Binding ScaleY, UpdateSourceTrigger=PropertyChanged, TargetNullValue='1'}" Style="{StaticResource InspectorTextBoxStyle}" PreviewTextInput="NumberOnly_PreviewTextInput"/>
                        </DockPanel>
                        <DockPanel Margin="2,0">
                            <TextBlock Text="Z" Width="12" Foreground="#4B4BFF" FontWeight="Bold" VerticalAlignment="Center" FontSize="10"/>
                            <TextBox Text="{Binding ScaleZ, UpdateSourceTrigger=PropertyChanged, TargetNullValue='1'}" Style="{StaticResource InspectorTextBoxStyle}" PreviewTextInput="NumberOnly_PreviewTextInput"/>
                        </DockPanel>
                    </UniformGrid>

                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
